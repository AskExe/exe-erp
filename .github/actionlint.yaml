self-hosted-runner:
  labels:
    - mac-fast
    - mac-local
    # build-my-erp-1 — the dedicated x86_64 VPS. Added so the release lane can
    # name that host unambiguously; plain `x64` also matches mac-vm-erp-1.
    - vps-x64
